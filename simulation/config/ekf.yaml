ekf_filter_node:
  ros__parameters:
    frequency: 30.0
    sensor_timeout: 0.1
    two_d_mode: true
    base_link_frame: base_link
    odom_frame: odom
    world_frame: odom
    publish_tf: true

    odom0: /odom
    odom0_config: [true,  true,  false,
                   false, false, true,
                   false, false, false,
                   false, false, false,
                   false, false, false]

    imu0: /imu
    imu0_config:  [false, false, false,
                   true,  true,  true,
                   false, false, false,
                   false, false, false,
                   true,  true,  true]

    use_control: false

